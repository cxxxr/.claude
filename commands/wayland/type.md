---
allowed-tools: Bash
description: Type text into the focused window using wtype
argument-hint: <text to type>
---

# Wayland Type

Type the following text into the currently focused window using `wtype`:

```
$ARGUMENTS
```

Use the command: `wtype "TEXT"`

For special keys, use: `wtype -k KEY` (e.g., Return, Tab, ctrl+c)
